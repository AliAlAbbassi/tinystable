{
  "name": "contracts",
  "version": "1.0.0",
  "description": "TinyStable smart contracts",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts --network sepolia",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "balance": "hardhat run scripts/check-balance.ts --network sepolia",
    "faucet": "hardhat run scripts/get-faucet-eth.ts --network sepolia",
    "tveth": "hardhat run scripts/check-tveth-balance.ts --network sepolia",
    "clean": "hardhat clean",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-viem": "^2.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.19.0",
    "viem": "^2.0.0",
    "typescript": "^5.0.0",
    "dotenv": "^16.0.0"
  }
}